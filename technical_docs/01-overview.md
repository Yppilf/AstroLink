# AstroLink Overview

AstroLink is a Django-based platform that provides:

- User management
- Role-based permissions
- Contract signing SES infrastructure
- Secure backup and restore functionality

The system is designed around a granular permission model where
access to functionality is determined by explicit permissions
rather than user type alone.